version: "2"

sql:
  - engine: "postgresql"
    schema: "schema.sql"
    queries: "query.sql"
    gen:
      go:
        package: "data_destination"
        out: ".."
        emit_exact_table_names: true