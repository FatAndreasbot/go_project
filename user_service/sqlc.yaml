version: "2"

sql:
  - engine: "postgresql"
    schema: "infra/postgresql/migrations"
    queries: "infra/postgresql/queries"
    gen:
      go:
        package: "sqlc_gen"
        out: "infra/postgresql/sqlc_gen"
        emit_exact_table_names: true